# Core DL stack
# NOTE: As of 2026-06, prebuilt torch wheels lag the very newest CPython.
# If you are on Python 3.14 and `pip install torch` fails, use a 3.11/3.12
# virtualenv (recommended for reproducibility) or the nightly index.
torch>=2.3
torchvision>=0.18

# Numerics / imaging
numpy>=1.26
pillow>=10.2
scikit-image>=0.22          # SSIM, classical metrics
glymur>=0.13                # JPEG 2000 (OpenJPEG) baseline
imagecodecs>=2024.1.1       # fast JPEG/JPEG2000 encode-decode for benchmarks
rasterio>=1.3               # read Sentinel-2 GeoTIFF / JP2 bands

# Experiment tracking & config
wandb>=0.16
pyyaml>=6.0
tqdm>=4.66

# Serving (Phase 5)
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.32
python-multipart>=0.0.9

# Dev
pytest>=8.0
